export enum SORT_OPTIONS {
  'Cũ nhất' = 'Oldest',
  'Mới nhất' = 'Newest',
  'Nhiều điểm nhất' = 'MostPoints',
}

export enum TabsEnum {
  Tab_1 = "",
  Tab_2 = ""
}

export enum USER_ROLES {
  USER = 'user',
  ADMIN = 'admin',
}

export enum EStatus{
  Pending = "Pending",
  Cancel = "Cancel",
  Error = "Error",
  Processing = "Processing",
  Done = "Done"
}
